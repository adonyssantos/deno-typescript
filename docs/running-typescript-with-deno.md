# Correr TypeScript utilizando Deno

Este proyecto corre con con [Deno](https://deno.land/). Instala Deno si aun no lo tienes.

Para correr el proyecto, ejecuta el siguiente comando:

```sh
npm run start
```
